---
"create-opentray": patch
---

`create --open` launches the materialized application after a successful apply — the wizard's Open App action as a CLI flag. The default stays create-only; `--open` with `--dry-run` stays dry.
