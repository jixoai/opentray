---
"opentray": patch
"@opentray/ext-webview": patch
---

Add common native tray-window auto-hide with keep-on-top and explicit opt-out semantics, keep comparator windows out of Windows task switchers by default, and expose the lifecycle policy through typed styles and capabilities.
