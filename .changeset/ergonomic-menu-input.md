---
"opentray": patch
---

Add app-facing ergonomic menu input for top-level `createTray` and `setMenu`, including string items, hyphen separators, tuple submenus, auto ids, and item-local `onMenuClick` callbacks while preserving pure protocol menu frames.
