---
"@opentray/ext-webview": minor
---

Add whole-window opacity to the WebView window style contract. `style.opacity` is a common shell-alpha field that composes with, but does not replace or imply, `style.background` material, blur, or transparent backing modes.
