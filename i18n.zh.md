<!--
正交意图（维护于 2026-07-21，用户原始需求包含 Windows 原生投影诊断、
“大家认知就是 pnpm install，剩下就应该正确工作”，以及稳定 App 入口复用
上一次 `process.argv` 或显式启动脚本）：
1. 固化用户对 Windows overlay-window-controls、控件着色与安全区测量的领域表达。
2. 区分“无窗口图标”和“无任务切换器入口”。
3. 固化 tray 寻址、placement 与 screen-center fallback 术语。
4. 固化 Windows 可见 frame、raw window rect 与 comparator 顶部 client 投影的差异。
5. 区分材质宿主残影与自定义 frameless 引入的非客户区外框残影。
6. 固化零修复安装、原生产物身份与调用方自有会话的生命周期术语。
7. 固化窗口在桌面 Shell 中的角色语义，以及 Windows/macOS 的不同原生投影。
8. 固化平台无关 Core 与 Darwin runtime `.app` carrier 的所有权边界。
9. 固化 App 启动命令、可变启动描述符与冷启动载体的边界。
妥协声明：该文件是项目级中文领域词汇 SSOT，必须持续聚合跨平台诊断词汇；按平台拆分会破坏
用户表达与规范词汇的一处映射，因此允许超过五个领域意图，但新增内容必须按独立章节隔离。
-->

# OpenTray 中文领域词汇

## 扩展仓库边界

Lynx 扩展由 [`jixoai/opentray-ext-lynx`](https://github.com/jixoai/opentray-ext-lynx)
独立维护。主仓只保留通用扩展 ABI、loader、`@opentray/spec` 与 `TrayHandle.extend`，不再构建、暂存或发布 Lynx 原生载体。

## 用户一手词汇

| 用户表达 | 规范词汇 | 工程含义 |
| --- | --- | --- |
| `overlay-window-controls` | 原生窗口控制覆盖层 | 页面拥有标题栏内容，系统拥有最小化、最大化、关闭按钮。 |
| 默认不应该有窗口图标 | 托盘工具窗口默认不进入任务切换器 | Windows 默认不产生任务栏和 Alt+Tab 入口；普通应用窗口需显式 opt-in。 |
| tray-icon 没有正确渲染 | 托盘 RGBA 投影失败 | 原生 HICON 的颜色 alpha 与单色 AND mask 必须共同保留可见像素。 |
| webview 没有完全铺满高度 | full-client 几何失败 | WebView2 未覆盖可用 client rect，通常残留 caption 或非客户区。 |
| frameless 下高度差 7px，但宽度只差 2px | 顶部单边 client 投影回归 | comparator 保留原生 resize frame 时，左右和底部继续采用系统 inset；顶部只保留 DWM 可见边框，不能保留 caption 高度。 |
| 正确寻址到 tray | tray 原生边界权威 | placement 使用 `backend.nativeTrayBounds`，不是估算位置。 |
| 锁定在窗口正中心 | screen-center fallback | tray bounds 不可用后触发的诚实降级，不是正确 tray placement。 |
| 大家认知就是 `pnpm install`，剩下就应该正确工作 | zero-repair install contract | 消费者只声明依赖并执行正常安装；清缓存、删除目录、手动重启与环境变量覆盖只能用于诊断，不能成为运行前置条件。 |
| `createTray()` 创建的连接应由谁关闭 | caller-owned broker session | 顶层 handle 拥有它创建的 broker 会话；`destroy()` 同时移除 tray 并关闭会话，创建失败也必须回收。 |
| 普通应用窗口还是托盘工具窗口 | app mode | 目标 WebView 契约的桌面 Shell 模式，由 `style.appMode` 声明为 `true` 或 `false`；不是某个平台的显示开关。 |
| `showInSwitchers` | 历史 Windows switcher projection 词 | 已从公共契约移除；统一使用 `style.appMode`。 |
| 窗口进入系统任务切换器 | app-mode shell participation | `appMode: true` 在 Windows 进入任务栏/Alt+Tab，在 macOS 由 Darwin carrier 投影到 Regular/Dock。 |
| 应用名称能不能动态修改 | bootstrap Shell name vs runtime logical name | macOS Dock/Cmd+Tab 名称由进程启动前写入 carrier 的 bundle identity 决定；`tray.app.setName()` 可更新 Core 逻辑身份，但不承诺动态改写当前进程的 LaunchServices 名称。 |
| 应用身份图标 | app icon | `appIcon` 属于 App identity，是由 `darwin/icns`、`windows/ico`、`linux/png` 或 `linux/svg` 组成的平台标准资产数组；它不继承 tray `icon`，也不是窗口 favicon 的实时别名。 |
| 默认变体 | default app icon variant | `variant` 省略时规范化为 `default`；显式 `"default"` 与省略完全等价。 |
| 图标变体 | app icon variant | App identity 的语义状态名；可表示 `light/dark`，也可表示垃圾篓的 `empty/files`，Core 不解释业务含义。 |
| 变体别名 | app icon variant aliases | 一个资产用 `variant: ["default", "light"]` 同时服务多个名称，避免复制资产条目和字节。 |
| Core 应不应该包含 `.app` | 内核与发布载体是两个层次：`opentray-core` 不含平台 bundle，OpenTray Darwin runtime 包必须携带共享 `.app`。 | Core owns identity protocol; `@opentray/darwin-*` owns broker plus carrier distribution. |
| `.app` bundle 应该放在哪里 | 共享的 Darwin 应用载体属于 runtime/release 层，不属于 `ext-badge` 私有扩展。 | `ext-badge` 只提供 badge/overlay 能力，不能成为普通 app-mode 的前置依赖。 |
| `.app` 稳定目录 | stable caller app bundle | Darwin runtime 默认使用 `~/.opentray/apps/<@scope+name>/<appName>.app`；可通过 `appBundle.path` 指定路径，相对路径从调用方 package root 解析。 |
| 重新生成还是使用预构建 bundle | managed vs prebuilt bundle | `reinitialize` 默认 `true`，只在稳定目录内替换 OpenTray 自有文件并最后提交 manifest；`false` 只读校验 broker/plist/icon/manifest 等预构建资产，冲突时返回 typed error；可变 launch descriptor 另行管理。 |
| 构建插件生成 `.app` | shared app-bundle generator | Vite、esbuild、webpack、tsdown 只适配生命周期，统一委托 `@opentray/packaging`，不能各自发明 carrier 布局。 |
| Vite 生成应用图标 | Vite app-icon generator | `@opentray/vite-plugin` 是可选工具，在 dev/build 的 Vite 预备阶段生成 ICNS、ICO、Linux 多尺寸 PNG 与可搬运 manifest；开发者也可以使用其它工具直接提供标准资产。 |
| 图标缓存为什么重新生成 | generator implementation cache identity | 缓存同时绑定源图、链接态生成器源码、已构建插件实现、渲染配方、编码器版本和输出路径；修改生成器源码并重建插件后必须失效。 |
| 图标像素正确但 Dock 尺寸异常 | app icon representation scale | 旁路预览和 Linux PNG 固定为 72 DPI；ICNS 必须用独立 @1x/@2x tag 表达 `1024px/512pt`、`512px/512pt`、`512px/256pt`，不能复制同一 PNG 或继承源图 25 DPI。 |
| 启动命令 / 入口命令 | app launch command | `appLaunch` 是直接执行的 `command + args + cwd` 向量，不是 shell 字符串，也不进入 Core App projection。 |
| 默认可空，使用 `process.argv` | automatic launch snapshot | `appLaunch` 省略或为 `null` 时，规范化为 `process.execPath + process.argv.slice(1) + process.cwd()`。 |
| 下次使用上一次的 `process.argv` | last launch descriptor | 成功完成本地 broker handshake 后，在稳定 Bundle 中原子覆盖 `Contents/Resources/opentray-launch.json`。 |
| 开发者预留启动脚本 | explicit app launch vector | 开发者显式提供可执行文件/启动器、参数与 cwd；OpenTray 不拼接 shell，也不持久化完整环境变量。 |
| 退出托盘后点击 Dock 入口 | Darwin carrier cold launch | broker/调用方退出后，由稳定 `.app` 严格读取描述符并启动一次调用方；这不等价于存活进程的 Dock reopen。 |
| 存活应用再次点击 Dock | warm app reopen | Darwin carrier 只发出 App 级 `reopenRequested`；不执行冷启动描述符，不创建第二个 consumer。 |
| 默认打开最近窗口 | app-mode MRU reopen projection | `@opentray/ext-webview` 在已 bootstrap 的 retained `appMode` 窗口中选择最近活跃者，顺序执行 `toVisible()`、`focus()`。 |
| 聚焦窗口 | explicit window focus | `WebviewWindowHandle.focus()` 与 `navigator.opentrayWindow.focus()` 是扩展拥有的底层能力，不属于 Core。 |
| 开发模式 Dock 启动 | supervisor launch vector | pnpm/Vite 项目显式保存 `process.execPath + [absolute npm_execpath, "--dir", absolute webui dir, "dev"] + repo cwd`，直接进入 Vite 所属 workspace；整个传递调用图不能依赖 Finder 中不存在的终端 PATH。 |
| 源码 link 的运行前准备 | linked runtime staging | 验收前先构建并 stage 同一源码图中的 facade、broker/carrier 和 native extension；这只属于 link 开发流程，registry 安装仍只需普通包管理器安装。 |
| pnpm dev 出现两个 Dock 图标 | Darwin App identity convergence | 当前 Bundle 成功握手并提交 descriptor 后，注销并清理无 owner 的旧同 AppId OpenTray Bundle；当前路径和存活 owner 不删除。 |
| `npm_package_json` 指向 `webui` | running consumer package identity | `npm_package_json` 是嵌套 runner 身份；默认以运行脚本最近的 package.json 作为稳定 Bundle 所有者。 |
| daemon 日志看不到错误 | persistent broker diagnostics | detached broker 默认追加到 `<runtimeDir>/broker.log`；`inherit` 继承终端，`ignore` 明确静默。 |
| 原生 broker 还在启动就被 SDK 终止 | native readiness budget | readiness 是覆盖 Darwin carrier/AppKit 冷启动的有界时间预算，不是乐观轮询次数；期间持续校验 PID 与产物身份，超时必须返回 broker 日志路径。 |
| Dock 点击后应用没有启动 | carrier launch diagnostics | `.app/Contents/Resources/opentray-launch.log` 记录 descriptor、spawn PID/错误与子进程 stdout/stderr，不持久化环境变量。 |

## 工程归一词汇

```text
raw window rect
  - invisible DWM resize border
  = visible frame / public bounds
```

| 词汇 | 定义 |
| --- | --- |
| 可见 frame | `DWMWA_EXTENDED_FRAME_BOUNDS` 返回的用户可见窗口边界。 |
| raw window rect | `GetWindowRect` 返回的原生边界，可能包含不可见 resize border。 |
| full-client | `WM_NCCALCSIZE` 不再为 caption 保留额外区域，WebView 填满 client。 |
| top-only client projection | 先由 `DefWindowProcW` 计算原生 resize frame，再只把 client top 调整到 `window top + DWMWA_VISIBLE_FRAME_BORDER_THICKNESS`；左右和底部 inset 不变。 |
| switcher visibility | 任务栏与 Alt+Tab 的参与策略，由 `style.appMode` 投影。 |
| package graph runtime | `MddBootstrapInitialize` 选中的 Windows App Runtime 实现 DLL 集合。 |
| CBS bootstrapper | `%SystemRoot%\SystemApps` 中可用于启动动态依赖解析的 bootstrap DLL；它不是 runtime DLL 身份。 |
| shell identity | Windows notification icon 的寻址键；当前依赖边界统一使用 `(HWND, uID)`。 |
| caller-scoped broker identity | broker endpoint 的调用方边界；Windows named pipe 不包含 `homeDir`，因此示例必须传入 caller label。 |
| Vite 生命周期权威 | 示例通过 Vite Node API 直接拥有 server instance；`listen()`、`resolvedUrls.local`、`close()` 分别是监听、URL 选择和退出清理的权威，不能解析 CLI 格式化输出。 |
| 单地址 loopback | 示例固定绑定 `127.0.0.1`；避免 Windows `localhost` 的 IPv6/IPv4 DNS 顺序让端口监听和 WebView 请求落到不同地址。 |
| size/move 交互 | `WM_ENTERSIZEMOVE` / `WM_EXITSIZEMOVE` 只投影交互状态；纯移动只通知 parent position，`WM_SIZE` 负责 `原生宿主 -> controller -> WRY child -> parent notification` 的有序提交。 |

## 禁止混用

- “无窗口图标”不能实现为 `WM_GETICON => 0` 后就宣称没有任务栏入口。
- CBS bootstrapper 所在目录不能自动推导为 `FrameworkUdk` 的正确加载目录。
- AppWindow overlay 不能描述为 MTA worker 的 fire-and-forget 操作。
- placement 的 screen-center fallback 不能描述为 tray placement 成功。

## Window Overlay 补充

| 用户表达 | 规范词汇 | 工程含义 |
| --- | --- | --- |
| 无边框还有旧 Windows 标题和边框 | 真正无边框 shell | Windows 无边框移除 `WS_THICKFRAME` 并关闭 DWM non-client 渲染；这不等价于改变 `background` 为透明。 |
| 默认行为不打破 | chrome 派生的可缩放默认值 | `resizable` 未声明时，framed 为 `true`，frameless 为 `false`；显式声明后切换 `frameless` 不覆盖用户选择。 |
| Application-level Soft-Resizing | 原生窗口层软缩放 | 页面只声明 `style.resizable: true`；六 CSS 像素边缘手势由注入层保留，HWND 负责捕获、物理矩形、约束、结束和事件。 |
| 无边框右侧滚动条与缩放是否冲突 | 原生滚动条与右侧软缩放并存 | 常规 Chromium 原生垂直滚动条不阻断右侧和右下六 CSS 像素软缩放；无需为此缩小 `body` 或改用自绘滚动条。仅当应用自己的边缘命中设计要避开该缩放带时，才由页面选择缩窄滚动容器或使用自绘/覆盖式滚动条。 |
| 示例退出后 `opentray.exe` 残留 | Exit 驱动专属 broker 终止 | Windows named pipe 半关闭可能延迟 `Disconnected`；已初始化会话收到 `ClientFrame::Exit` 并完成 core 清理后，专属 GUI broker 必须直接退出事件循环。 |
| Windows 上 OpenSpec 测试全部子流程失败 | 直接 Bun 子进程 + 文件描述符捕获 | 测试不能依赖 `/bin/sh` 重定向；用 `process.execPath` 启动 Bun，并以临时 stdin/stdout/stderr 文件描述符保持确定性捕获。所有面向 TOC、JSON 和断言的相对路径统一为 `/`，不能泄漏 Windows `\\`。 |
| Windows 打包测试没有可执行位 | 平台文件模式语义差异 | `executable: true` 仍要求 runtime host 成功复制；仅 POSIX 断言 `stat().mode & 0o111`，Windows 只验证文件存在。 |
| Windows esbuild 入口变成 `/E:/E:/...` | URL 与宿主路径边界 | 传给 bundler 前用 `fileURLToPath()`；动态导入使用 `pathToFileURL(path).href`，不能以 `.pathname` 重新喂给 URL 转换。 |
| Chrome PWA 一样的 window-overlay-controls | Windows 原生 caption-button 着色 | `windowControlsOverlay` 可从布尔值扩展为带 `backgroundColor` / `symbolColor` 的对象；Windows 仅接受不透明 `#RRGGBB`，并通过 `AppWindowTitleBar` 应用。 |
| 控件宽度要正确测量 | AppWindow 标题栏安全区 | `LeftInset`、`RightInset`、`Height` 是 page 可用标题栏矩形的权威；不能由 DWM 按钮矩形或固定按钮数量反推。 |
| macOS 不需要按钮背景色 | 平台原生控件保真 | macOS 接受同一声明但保持透明交通灯控件，不模拟 Windows 的不透明按钮底色。 |

## Windows Composition Recovery

| 用户表达 | 规范术语 | 工程含义 |
| --- | --- | --- |
| `原生窗口残影` | native host/DWM material residue | 可见残影属于顶层 HWND、DWM 材质或重定向表面，不应先归因于 DOM/WebView 页面像素。 |
| 残影很像 Win7 的窗口，外圈有边框 | stale non-client-frame residue | 旧标题栏或窗口 frame 像素残留在顶层 HWND/DWM 表面；它与 WebView 页面残影、材质主体残影必须分开诊断。 |
| 残影得比较纯粹 | comparator-pure host residue | 比较器只暴露双方共有的材质宿主残影，不能额外混入 OpenTray 自定义 frameless、软缩放或 full-client 投影产生的外框变量。 |
| `只修改原生窗口` | native host surface recommit | `clearWhiteBlock` 只重新提交 HWND 自己的背景表面，不切换 shell 状态、不修改窗口尺寸，也不请求 WebView2/WRY bounds。 |
| `原生宿主完整刷黑` | material host black base | Mica/Acrylic/Tabbed/semantic blur 在 `WM_ERASEBKGND` 和 `WM_PAINT` 中用 `BLACK_BRUSH` 填满完整 HWND client；黑色是 DWM 材质的合成底，不是覆盖 WebView 的黑层。 |
| WebView 只占窗口 `1/3`，其余区域仍有残影 | parent-surface ownership evidence | 残影与 WebView 子窗口覆盖范围无关，责任边界位于顶层 HWND/DWM redirection surface。 |
| `先完成原生窗口，再渲染 WebView2` | parent-before-child commit order | 样式事务与 `WM_SIZE` 必须先提交 parent host surface，再提交 controller bounds、WRY child bounds 和 parent-position notification。 |
| `窗口创建方式不大一样` | cold-start HWND construction topology | 冷启动必须按 `隐藏 HWND -> 原生材质/绘制权 -> 最终初始尺寸 -> 透明 WebView2 child -> show` 完成；不能把 retained style update 当作首次构造。 |
| `webview要完全透明` | transparent WebView control plane | WebView2 controller 从创建时即支持 alpha，页面的 html/body/root 与按钮外像素全部透明；只有控件自身贡献可见页面像素。 |
| 显式 DLL 后托盘无响应 | source broker affinity | `OPENTRAY_EXT_PATH` 只选择扩展 DLL；源码示例仍需选择同一源码修订的 `target/debug` broker，除非调用者也显式设置 `OPENTRAY_BROKER_BIN`。 |
| `webview-control` 任何动作都会异常闪退 | AppWindow post-WebView COM ordering | Win32/DWM 材质与初始几何先完成；AppWindow titlebar overlay 必须等 WebView2 在 HWND 线程建立 COM 后再初始化，并在首次最终 bounds/show 前完成。 |
| `broker connection closed` 多次重复显示 | terminal single-flight event polling | 多个窗口事件 listener 共用一个不重叠的 drain 请求；transport 失败后停止 interval，只报告一次错误。 |
| `完全一样的底层路径` | shared comparator host topology | 两个源码示例共享 HWND/DWM、初始几何、扩展样式、非客户区、frameless 与 resize 决策；`--no-overlay` 是完整 A/B，overlay 只允许作为 WebView2 附着后的额外 AppWindow 阶段。 |
| `Show|Hide Example、Quit Demo` | retained tray menu projection | 主菜单文案由原生 operational visibility 投影：可见时 Hide、隐藏或最小化时 Show；Quit 销毁 retained WebView 并关闭 runtime。 |

## 托盘窗口生命周期补充

| 用户表达 | 规范词汇 | 工程含义 |
| --- | --- | --- |
| 最小化后托盘还显示 Hide Window | operational visibility authority | 菜单必须订阅 `visibleChange` 并以 `isVisible()` 为权威，不能维护私有 shown/hidden 布尔值。 |
| 默认不在系统任务栏显示 | switcher exclusion invariant | `style.appMode: false` 在生产与 comparator 拓扑中都投影为 `WS_EX_TOOLWINDOW` 且移除 `WS_EX_APPWINDOW`。 |
| 失去焦点自动隐藏 | native auto-hide | 公共 `style.autoHide` 默认 `true`；原生 blur 时仅在 `autoHide && !keepOnTop` 下隐藏 retained session。 |
| 保持置顶就不要自动隐藏 | pinned auto-hide suppression | `keepOnTop: true` 同时表达置顶与抑制原生 auto-hide，但不改变 `autoHide` 的声明值。 |
| 程序自己决定何时隐藏 | application-owned dismissal | 设置 `autoHide: false`，保留窗口给页面动画、确认流程或诊断工具自行关闭。 |
| `keepOnTop` 不是普通应用窗口替代方案 | independent z-order capability | `keepOnTop` 只表达 z-order；普通应用参与任务栏、Alt+Tab 或 Dock 必须使用 `appMode: true`。 |

## Window Shell Mode 补充

```text
appMode: true
        |
        +-- Windows: WS_EX_APPWINDOW, taskbar, Alt+Tab
        +-- macOS: Darwin carrier Regular demand, Dock/Cmd+Tab

appMode: false
        |
        +-- Windows: WS_EX_TOOLWINDOW, no taskbar/Alt+Tab
        +-- macOS: Accessory demand when no app-mode window remains
```

`appMode` 是源事实；原生平台字段只是投影。模式不隐含 `frameless`、`keepOnTop`、`autoHide`
或材质。macOS 的 activation policy 属于进程级投影，因此必须按 app/tray/session 聚合，不能
由一个窗口关闭回调清空其它仍存活窗口的应用身份。

`appIcon` 是 App identity 图标，不是 WebView window icon。它不继承 tray icon；省略时使用
carrier/可执行文件/系统默认身份。窗口 icon 后续的 favicon 或 `setIcon()` 变化不能反向修改
Dock/taskbar 身份。

`App Icon Variant` 是 App identity 的语义状态，不是主题枚举。`variant` 省略即 `default`，
也可用数组声明别名。Core 保留资产目录和当前名称；`tray.app.setAppIcon("files")` 只切换
已声明投影。应用若要跟随主题或业务状态，可通过自己的类型安全 IPC 调用 App handle，
`ext-webview` 不拥有该能力。

## macOS Runtime 兼容性诊断

| 用户表达 | 规范词汇 | 工程含义 |
| --- | --- | --- |
| `hideWindow` 突然失效，daemon restart 后恢复 | retained broker artifact skew | 同一 endpoint 上存活的 broker/native extension 可能来自较早的依赖图；restart 替换了运行时载荷，但不能单独证明 AppKit hide 有缺陷。 |
| `extension ... returned code 1` | native extension rejection without detail | `code 1` 仅表示 `EXT_ERR_REJECTED`；当前 broker 丢失了 dylib 的具体错误文本，必须结合 broker stderr 或诊断协议继续定位。 |
| 窗口仍有 focus/blur，但 `isVisible` 被拒绝 | command-surface skew evidence | 旧命令链仍然存活而新命令不可用，优先核对 facade、broker 与 dylib 的命令面版本，不能先归因于 `NSWindow.orderOut`。 |
| restart 就解决了 | runtime replacement evidence | restart 同时替换 broker 进程、动态库实例与 retained window；它缩小了故障边界，但不是根因名称。 |
| broker 还活着 | liveness-only evidence | PID 与 endpoint 只能证明进程存活；`already-running` 还必须要求 caller-scoped ready metadata 与本次安装解析出的 broker 路径、SHA-256 identity 完全一致。 |
| 核心 SDK 的声明里没有窗口方法 | extension capability boundary | `opentray` 只声明通用 tray/runtime 契约；WebView 窗口方法、事件和样式属于 `@opentray/ext-webview`。 |
| manifest 已升级但类型检查仍像旧版 | installed dependency graph drift | `package.json` 与 lockfile 不是安装事实；以 `pnpm why`、解析版本和模块 realpath 判定当前编译器读取的声明。 |
| lockfile 是 `0.14.4`，运行时却像 `0.12.0` | historical top-level native artifact shadowing | 旧 broker 曾优先命中工作目录顶层平台包；当前正常路径由 facade 依赖闭包解析唯一 dylib，并在 init 前校验内嵌 identity。若仍出现该现象，先确认运行的 SDK/broker 是否仍为旧产物。 |
| `pnpm why` 全部正确但原生命令仍被拒绝 | loaded-library truth | 依赖图不能证明 `dlopen` 结果；macOS 以 `lsof -p <broker-pid>` 的 dylib 路径为运行事实，再读取同目录 manifest 并比较产物哈希。 |
| broker 自己猜 `node_modules` 布局 | package-topology reconstruction | Rust broker 不应重建 npm/pnpm/Yarn/Bun 的解析规则；Node SDK 以 facade package 为解析起点得到精确 platform artifact 路径。 |
| 找到第一个 dylib 就加载 | existence-only discovery | 文件存在不等于契约兼容；loader 必须先验证内嵌 artifact identity 与 facade 期望，再 init 或继续检查后续候选。 |
| 扩展只返回 `code 1` | structured native rejection | ABI 必须提供 category/message，并由 broker 在 init、command、session cleanup 失败后读取和释放；数字结果码只保留错误大类。 |
| 平台包版本正确但里面是旧 dylib | release artifact evidence mismatch | 原生 runner 读取 dylib 内嵌 manifest 并记录 SHA；staging 先核对下载字节、facade contract 与平台 target，任一不一致都在 publish 前失败。 |
| Windows arm64 发布校验无法 `dlopen()` | same-target native manifest inspection | Bun 的 Windows arm64 构建可能禁用 TinyCC/FFI；扩展清单必须由与扩展同目标编译的原生检查器读取，不能降级为跳过校验。 |
| changeset 已升版但 native 先编译了 | release-source skew | versioned release source 必须先生成；stable commit SHA 或 alpha patch 是 native matrix 与 publish job 的共同输入，不能分别计算版本。 |
| source example 报 `path must be shorter than SUN_LEN` | Unix endpoint path budget | 临时 home 与 caller label 复用同一短 invocation token，保留进程隔离但不重复描述性长前缀；完整 socket 路径必须低于原生 `sun_path` 字节上限。 |
| 依赖升级后还连到旧 broker | stale broker reuse | endpoint/PID 只证明存活；握手必须校验 broker artifact identity，不一致时自动有界替换。 |
| 同一个版本为什么还复用旧 broker | broker artifact identity reuse gate | SDK 每次启动只解析一个 broker 可执行文件并计算目标平台 + SHA-256；ready.json、ready frame 和握手必须完全一致，否则在锁内有界替换。 |
| 顶层旧平台包遮蔽了当前包 | facade-relative exact resolution | Node 从 facade 自身依赖闭包解析唯一平台包；Rust 不再猜 node_modules 拓扑，也不回退到第一个存在的原生文件。 |
| Windows 源码重编译会影响别的托盘 | caller-scoped source target | 每个 caller 使用独立 Cargo target 目录，源码重编译不能按共享 executable 路径批量终止其他 caller 的 broker。 |
| 诊断目录第一个 dylib 过期但后面有新 dylib | diagnostic candidate continuation | 仅显式诊断候选允许记录 missing/unreadable/identity mismatch 后继续；facade 的绝对路径仍只加载一次，不能回退。 |
| 本地测试子进程 stdout 为空 | file-backed Bun child capture | OpenSpec 测试直接 spawn `process.execPath`，用 `Bun.file` 文件 sink/source 捕获三条流；不通过 `/bin/sh`，兼容原生 Windows。 |
| 本地验证通过但发布仍可能装错 | native packed-consumer release gate | release matrix 在目标 native runner 上分别安装真实 tarball 的 pnpm/npm consumer，创建 tray 并通过 public `loadExtension` 验证 broker/native identity 后才允许 publish。 |

```text
facade package
      |
      v
Node package resolution
      |
      v
exact platform package + expected identity
      |
      v
broker validates embedded identity -- mismatch --> reject before init
```

```text
endpoint + live PID
        |
        v
ready identity == currently resolved broker identity ?
        | yes                         | no / missing
        v                             v
      reuse                  bounded automatic replace
```

## WebView2 ??? Profile

| ???? | ???? | ???? |
| --- | --- | --- |
| 'pnpx' ??????? | executable-path-independent WebView2 profile | WebView2 ???????????? broker ???????????????????????? |
| WebView2 profile | WebContext-owned user data directory | Windows ??? 'WebContext' ?????? profile??????????? WebView? |
| ??????? | bounded profile path | ???? '<home>/.opentray/webview/<package-version>/<caller-label>'???? exe ??? '.WebView2' ??? |
| profile ???? | profile-path diagnostic | WebView2 ?????????????????? profile ?????????????????? |
