# @opentray/darwin-x64

OpenTray runtime artifact package for macOS Intel.

This package is installed as an optional dependency by `opentray`. CI stages the release-built runtime artifact into the platform artifact path before npm publish; source control does not commit the generated binary.
