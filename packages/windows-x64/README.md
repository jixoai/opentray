# @opentray/windows-x64

OpenTray broker binary package for Windows x64.

This package is installed as an optional dependency by `opentray`. It will contain the release-built broker binary for `windows-x64` once Rust builds are introduced.
