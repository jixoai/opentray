// Minimal entry for the OpenTray tsdown adapter build example.
// Real builds import runtime/host code; this only proves the staging contract.
export const main = (): string => "opentray-tsdown-build-example";
