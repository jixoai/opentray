// Minimal entry for the OpenTray webpack adapter build example.
// Kept as plain JS so the real-build test needs no TypeScript loader dependency.
module.exports = { main: () => "opentray-webpack-build-example" };
