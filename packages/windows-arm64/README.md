# @opentray/windows-arm64

OpenTray broker binary package for Windows ARM64.

This package is installed as an optional dependency by `opentray`. It will contain the release-built broker binary for `windows-arm64` once Rust builds are introduced.
