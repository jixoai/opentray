# @opentray/windows-arm64

OpenTray broker binary package for Windows ARM64.

This package is installed as an optional dependency by `opentray`. CI stages the release-built broker binary into `bin/opentray.exe` before npm publish; source control does not commit the generated binary.
