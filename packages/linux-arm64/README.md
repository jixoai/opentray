# @opentray/linux-arm64

OpenTray broker binary package for Linux ARM64.

This package is installed as an optional dependency by `opentray`. It will contain the release-built broker binary for `linux-arm64` once Rust builds are introduced.
