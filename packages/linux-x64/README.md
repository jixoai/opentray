# @opentray/linux-x64

OpenTray broker binary package for Linux x64.

This package is installed as an optional dependency by `opentray`. It will contain the release-built broker binary for `linux-x64` once Rust builds are introduced.
