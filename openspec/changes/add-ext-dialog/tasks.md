# add-ext-dialog — Tasks（R2 修订版）

> 规范附录：`plans/design-reference.md` 是实现准绳——DeferredOperation 主模型、
> DeferredCompletionPort/poll_owner ABI、per-owner STA worker、单会话运行时裁决、embedded
> 身份链、真实 pack 证据、TypedExtensionError + async getBackend 全部以其为准。
> 评审记录：R1 4.0 → R2 5.0（NO-GO）；本版吸收 R2 P0-1/2/3/5/6/7 与 P1-1/2/4/5。

## 1. Alignment

- [ ] 1.1 plan 索引与 design-reference 一致；validate 通过；每个 checkbox 仅由当前工作上下文完成并验证后勾选。

## 2. BDD Contract — 批次 A（协议 + 共享基建；独立可审单元）

- [x] 2.1 `@opentray/spec` + opentray-spec：`ExtCommandAccepted`/`ExtOperationTerminal`（payload 恒带结果）全量 server frame 与 parser 真值；operation 归属 `(sessionId, instanceGeneration, operationId)`；`TypedExtensionError {code, message, details}` discriminated union；`CommandScope` 注入；Dialog 命令/选项/结果类型 + BackendCapabilities 共享 schema + exhaustive fixture；**protocol version 1→2 全矩阵同一提交门**（Node/broker/socket endpoint/ready metadata/旧协议拒绝）；**共享 payload 上限常量 `EXTENSION_EVENT_RECORD_MAX_BYTES`（opentray-spec 与 @opentray/spec 同值导出，event_hub 私有常量迁出为唯一真值）**；单测（repr/schema 冻结）。
  - 批次 A 证据（Rust 侧，commit ce4d7a98）：opentray-spec `PROTOCOL_VERSION=2` + `ExtCommandAccepted`/`ExtOperationTerminal`（`ExtOperationPayload` result|error 判别，round-trip parser 真值测试，未知 kind 拒绝）；version-1 Init 拒绝（spec `is_supported_protocol_version(1)==false` + core broker `version_one_init_is_rejected_as_incompatible`，endpoint/握手/health 字面值随 p2 全量更新）；`CommandScope {appId,trayId,sessionId,instanceGeneration}` 注入 envelope（`commandScope` 可选字段，legacy 反序列化不变）；`TypedExtensionError {code,message,details}` wire shape；`ExpectedExtensionIdentity` 可选 `sha256`/`buildIdentity`（旧帧兼容 round-trip 测试）；`EXTENSION_EVENT_RECORD_MAX_BYTES=64KiB` 公开常量，event_hub `EVENT_DATA_MAX_BYTES` 改为别名。opentray-spec 59 tests。**注记**：Dialog 命令/选项/结果类型与 BackendCapabilities 的 TS 共享 schema、@opentray/spec 同值导出、Node 侧 v2 切换属波 2（分支内已知中间态，绿门 4）；operation 归属键由 core registry（operations.rs）以 `(sessionId, instanceGeneration, operationId)` 实现。
  - 实现复核修复（Rust 侧，2026-09-17，commit 730295af）：`ServerFrame::Error` 增可选 `details`（skip-when-absent，旧帧字节不变、旧端反序列化不变）——同步 typed 错误经 Rust→server frame 携带与 deferred 终帧同构的 discriminated JSON（复核 P1-4 wire 半边，含 details round-trip 与 legacy 无 details 反序列化测试）；FFI `ExtensionErrorDetail` 增可选 `details`（JSON DTO 兼容扩展，非 repr(C) ABI 变更）+ `ExtensionErrorSlot::fail_with_details`；operationId 冻结 wire 形成为 parser 真值：`is_valid_operation_id`（16 位小写 hex）+ 两 deferred 帧反序列化强制（空/大写/非 hex/长度错全拒，对抗测试）；批次 A 注释非 ASCII 标点归一。opentray-spec 61 tests。
- [x] 2.2 可选版本化 ABI 符号：**`opentray_ext_command_v2`（独立符号；**loader 四格符号矩阵：仅 V2=全能力（旧符号不再 required）/仅 V1=恒 Immediate 无 UB/双符号=用 V2/双缺失=abi_incompatible；探测结果入能力诊断与 load 日志**；`ExtCommandDispositionV1` repr(C)（tag 0/1 数值冻结、未知 tag 或非零 reserved → typed 拒绝、value 无 bytes 成员——out_events 是唯一事件/结果通道，Deferred 时必空否则结构化丢弃）+ `ExtDeferredPortV1` repr(C) + **按值 attach**（扩展复制 struct 只留 port_data，stale struct 指针不可保留，attach-teardown fixture）+ size_of/offset_of fixture）** + `opentray_ext_attach_deferred_completion_port_v1`（EventPort 生命周期模式：immutable host 状态/version+struct_size/bounded-copy submit/CLOSED+INVALID_HANDLE+OVERSIZED 返回码/LoadExt ACK 后打开/清理前 revoke）+ **`poll_owner(operation) -> Pending{next_deadline, wake_reason}`（poll 不携带终帧；唯一终帧源 = port submit）**（owner-loop 调度器：合并 DialogPollDue(generation)、WaitUntil(min deadline)、deadline 提前的 re-arm、每迭代 ≤4 owner × 1 步进配额）；opentray-spec 常量与 stub FFI 决策表测试（同 EventPort 测试形态，含 Immediate 双输出负例）。
  - 批次 A 证据（commits ce4d7a98/b069d2b9/f3b7f5dc）：`ExtCommandDispositionV1`/`ExtDeferredPortV1` repr(C) + tag 0/1 冻结常量 + size_of/offset_of fixture；handle 经 host 预填充 disposition 单向下发（stub 测试断言扩展观察到 Deferred 种子与签发 handle）；决策表 stub 测试：Immediate 双输出负例（tag=0 且 value 非零 → typed abi_incompatible）、未知 tag、非零 reserved、Deferred handle 回显不符、Deferred 非空 out_events（buffer 释放+结构化诊断+不投递）、FFI 失败码路径；四格符号矩阵测试（`CommandSurfaceCell` 四态 + `REQUIRED_EXTENSION_SYMBOLS` 移除旧 command 符号 + 诊断标签 v2/no-deferred/missing 入 load 日志）；DeferredPort：按值 attach + attach-teardown fixture（port_data 存活长于 struct）、ACK 前后 CLOSED、revoke、OVERSIZED（先于任何字节拷贝）、伪造/重放/错 owner → INVALID_HANDLE、非法 payload 拒绝、bounded queue backpressure；返回码 OVERSIZED=6/INVALID_HANDLE=7 冻结测试；调度器骨架（dialog_poll.rs）：合并 min_deadline/WaitUntil 输入、提前 re-arm 信号、≤4 owner×1 步配额、generation-token 失效，winit（macOS/Windows owner loop）与 Linux 阻塞环双端接线。**注记**：`poll_owner` 的 FFI 符号名设计未冻结，属批次 B dialog 扩展 crate 的 producer 侧；批次 A 落地的是 broker 侧调度契约（poll 永不产出终帧——终帧唯一通道 port submit 已由 registry CAS 实现并测试）。
  - 实现复核修复（2026-09-17，commit 0401d4d0）：P1-1——DeferredPort open/revoke/enqueue 收敛到单一 per-port lifecycle 临界区（phase+session+queue 一把锁；入队权威检查在锁内，revoke 后队列零突变、OPEN 复活不可表达），barrier 控制的 open-vs-revoke（64 轮，无 OPEN 复活、revoke 后 submit 恒 CLOSED）与 submit-vs-revoke（32 轮，每个 EXT_OK 恰对应一条队列记录）确定性竞态测试（无 sleep）；P1-3——无 deferred port 的 V2 实例答 Deferred → typed `deferred_port_unsupported` 拒绝（stub + 真库双测试，操作随错误路径 retire，不再永久 pending）；P1-5——测试内 cc 编译四格真实 shared-library fixture（V2-only/V1-only/both/neither，-D 宏控格）：真符号拼写/repr(C) 布局/调用约定、seeded handle 经真 FFI 边界观测、V1 格以 V1 约定被调（marker+events 双证）、双符号走 V2、双缺失 abi_incompatible、端到端 deferred 事务经真 C→Rust port submit 结算恰一帧（unix 宿主；Windows 真库证据仍按平台法则单列）；P2 零长——`free_owned_bytes` 集中所有权清理，非空即释放（events/error/disposition 清理三路径，计数 free 测试）。
- [x] 2.3 broker（opentray-bin/core）：operation registry + owner loop CAS 结算（重复/错 owner/旧 generation 无状态丢弃+诊断）+ session writer 路由（不承诺 event barrier）；CommandScope 注入与 registry 键升级；`Send` 安全裁决（UI-affine 实例 owner-thread registry，不依赖 unsafe Send）；**LoadExt 携带 expected sha256/buildIdentity，dlopen 前重 hash，native manifest 库开后 init 前校验**（顺序以 design §6.4 为准）。
  - 批次 A 证据（commits b069d2b9/f3b7f5dc）：core `DeferredOperationRegistry`（派发前预注册→Immediate/失败 retire；owner-loop 一次性 CAS：Settled/Duplicate/ForeignOwner/StaleGeneration/UnknownHandle 全决策表测试；handle 为不可预测 u64 nonce，operationId=16 位 hex wire 形式，handle 0 永不签发；session purge）；终帧仅向仍匹配 session writer 投递（`drain_deferred_terminals`，duplicate/foreign/stale/unknown 结构化诊断丢弃，无 event barrier 承诺）；CommandScope 注入 + registry 键升级；身份链按 §6.4 顺序：dlopen 前对已解析路径重算 SHA-256 比对（真实字节替换 adversarial 测试）→ Library::new 后 init 前 manifest 校验新增 buildIdentity 比对（expected 提供时）。core 44 tests、bin 101 tests、spec 59 tests 全绿；clippy 告警集为改动前基线严格子集（另修掉一个基线告警）。**注记（Send 裁决）**：批次 A 引入的全部跨线程面（deferred port submit、registry）只携带进程级存活的 Send+Sync 状态（port_data/handle/payload），实例指针零跨线程——UI-affine 实例在现行单 owner-loop 下不可移动；`ExtensionInstance: Send` blanket 与 `unsafe impl Send` 的移除、owner-thread registry + 线程亲和断言随 3.3（win32 STA worker 使移动首次可表达）落地，本批以构造性不移动 + 该注记作为批次 A 裁决记录。
  - 实现复核修复（2026-09-17，commits 75d30a76/0401d4d0/7f9d11cc）：P1-7——handle 改为每 operation 独立 keyed-random u64（表锁内对 0/碰撞重抽，删除可预测 counter-over-base），非等差序列回归测试 + operationId 保持 16 位小写 hex 投影；P1-2——两个 owner loop（winit + Linux 阻塞环）的 Exit 路径经 closing-aware `drain_deferred_terminals` 在任何 deferred drain 前先 purge 该 session（§5.7 裁决 7：close 零终帧投递），回归测试证明关闭 session 零 `ext-operation-terminal` 帧且幸存 session 正常结算；P1-4——`ExtensionError::Detailed` 携带 details、`kernel_error` 映射进 error 帧（correlated dispatch 测试断言 code/message/details wire 同构 + 失败派发 retire 预注册 operation）；P1-8——drain 在仍有队列时再投递 wake（event_hub rewake 模式），多 port 超配额测试（3 port × 30 条 vs 64 条全局配额）排空到零且空 hub 不自旋；Darwin app_launch 回归定位为 harness 时序（1s 轮询窗在并行负载下丢失，非批次 A 改动——carrier 冷启动路径不经过 broker/protocol 代码且 git diff 证实批次 A 未触碰；独立 12 轮探测 marker 恒出现、载入耗时最高 2.5s），修复为 10s 有界窗口 + 超时 panic 附 carrier log 尾部，10/10 连续通过（修复前 ~25% 失败）。修复后绿门：spec 61 / core 46 / bin 109 unit + app_launch 3/3 + backend_composition 2 全绿；clippy 告警分布与修复前基线逐条相同（零新增）。
- [x] 2.4 Node（packages/cli）：pending-until-final 状态机、导出 typed error class（code/details/cause）、markDead 断连 typed `dialog_transport_closed` 拒绝；确定性测试（Node+Bun 双跑）：accepted 后两普通请求/重复终帧/completion-Exit race/disconnect 前后/旧 generation/wrong owner。
  - 批次 A 证据（TS 侧，commits dceacd42/129599d8）：@opentray/spec `PROTOCOL_VERSION=2`（`isSupportedProtocolVersion(1)==false` 拒绝测试 + endpoint/socket `p2` 字面值全量更新，对齐 Rust）；`ServerFrame` += `ext-command-accepted`/`ext-operation-terminal`（`ExtOperationPayload` result|error 判别 parser 真值，未知 kind/缺 error/缺 value 拒绝）；`TypedExtensionError`/`CommandScope` camelCase wire shape + guard；`ExpectedExtensionIdentity` 可选 `sha256`/`buildIdentity`（旧帧缺省 round-trip）；`EXTENSION_EVENT_RECORD_MAX_BYTES=65536` 同值导出（单测断言数值）。local-broker pending-until-final：Accepted 不结算、operationId→requestId 绑定、Terminal 恰一次结算（result → 以关联 `ext-operation-terminal` 帧 resolve，error → typed reject）、重复/未知 operationId 终帧与未知 requestId acceptance 无状态丢弃（镜像 broker CAS）；markDead 对已 accepted 的 pending operation 以通用 `extension_transport_closed` typed 拒绝（`ExtensionOperationError` code/details/cause、instanceof 可判，core 无扩展名分支；facade→`dialog_transport_closed` 公开码映射属批次 C），普通 pending 请求保持 `broker connection closed` sentinel；`client.ts` `requestIdOf` 穷尽接线。确定性测试 Node+Bun 双跑（spec 146/146 + cli 146/146 双绿）：init 携带 protocolVersion 2、accepted 后两普通请求正常完成、deferred 命令夹在两普通命令之间恰一次结算、重复终帧/foreign 终帧丢弃且连接存活、断连前后（typed vs sentinel 双臂）、error payload typed reject；旧 generation/wrong owner 在 TS 侧表现为未知 operationId 终帧丢弃（broker 侧 CAS 已由 2.3 Rust 测试覆盖）。
  - 实现复核 r1 修复（2026-09-17，P0-1/P1-4/P1-9/P2-hex TS 侧）：公开 deferred 终帧消费契约落地——`requestExtension`/`commandExtension`/`TrayExtensionContext.request` 以判别 union `ExtensionRequestResult`（`{kind:"immediate",events}` | `{kind:"terminal",operationId,value}`）结算，result terminal 的 `payload.value` 直达公开面（复核报告 P0 复现命令由 throw 变 resolve，批次 C facade 可直接消费 `value`）；error terminal 保持 `ExtensionOperationError` typed reject。同步错误同构：`ServerFrame::error` wire 增可选 `details`（JSON 对象，缺失=无结构化载荷；parser 拒 null/标量/数组），Node 侧以 `BrokerServerError`（code/details，client `expectResponse` 与 local-broker dispatch 两路）替换裸 Error——同步/deferred 两路共享 `{code,message,details}` 家族，socket 真连接 round-trip 测试（instanceof/code/details + 连接存活）落地。Ready 帧协议版本校验：`LocalBrokerConnection.init` 对 `frame.protocolVersion !== init 发出版本` 抛 `BrokerProtocolVersionError`（含双侧版本），wrong-ready-version 测试补齐（结构性 parser 校验与 p2 endpoint 矩阵保留）。operationId wire 谓词冻结：`OPERATION_ID_PATTERN=/^[0-9a-f]{16}$/` + `isOperationId` 导出，accepted/terminal 帧 parser 强制（空/大写/非 hex/错长对抗样本全拒）。
- [x] 2.5 opentray SDK：`NativeExtensionEmbeddedArtifact`——containment + embedded staging manifest 身份链（hash 计算 + buildIdentity 纳入 expected identity）+ 四类结构化错误 + adversarial 四族（含替换真实 library bytes）。
  - 批次 A 证据（commit a9a40637）：`kind: "embedded"` resolver——绝对与 `..` 穿越 libraryPath 在任何文件系统访问前词法拒绝；realpath containment（root 与 candidate canonicalize，path API 而非字符串拼接，Windows 交叉安全）拒 symlink 逃逸；身份链按 §6.4 可实现序：读取并校验 `platforms/manifest.json`（facade version 与 package.json、contract fingerprint 与 contract.json、per-target path/sha256/buildIdentity schema、未声明 target 拒绝、descriptor/manifest path 一致性）→ 计算真实库字节 SHA-256 比对 → `expectedIdentity` 携带 `sha256`/`buildIdentity` 进 LoadExt（Rust 侧序列化字段与 broker dlopen 前重 hash 已由 2.3 就绪）。四类 typed 错误 `NativeExtensionEmbeddedArtifactError`（target-unsupported/path-outside-facade/manifest-invalid/library-unreadable，稳定 code + reason + target + facadePackageJsonUrl）。adversarial 四族测试：路径穿越+绝对路径、symlink 逃逸（manifest 字节诚实仍拒）、真实 library 字节替换（hash 不匹配必拒）、manifest skew（版本/指纹/路径/畸形 JSON/缺字段/多余 target/manifest 缺失）；cli 146/146 Node+Bun 双绿。
  - 实现复核 r1 修复（2026-09-17，P1-6 TS 侧）：embedded staging 冻结四目标矩阵强制——导出 `NATIVE_EXTENSION_EMBEDDED_TARGET_MATRIX`（darwin-arm64/darwin-x64/win32-arm64/win32-x64）与 `NativeExtensionEmbeddedMatrixTarget`；descriptor 类型收紧为 `Record<matrixTarget, …>`（Partial 移除，不完整 catalog 类型不可表达，`@ts-expect-error` 类型级测试佐证）；运行时 resolver 要求 manifest 声明集合对矩阵精确完备（缺任一格 → manifest-invalid 且错误消息点名缺失格，四格各有独立测试臂；矩阵外 key → manifest-invalid；release staging 校验分层不变）；fixture 从单 darwin-arm64 扩为全四目标（跨目标解析成功臂 + linux 目标 unsupported 臂更新）；矩阵内 descriptor 查找经类型守卫，非矩阵 target 走 target-unsupported。
- [x] 2.6 `scripts/check-pack-size.mjs`：真实 `npm pack --json --pack-destination <temp>`（stat tgz 字节、npm 版本、packlist、目标 hash）；≥2MB 警告、>3MB 失败；`--dry-run` 仅开发预警；fixture 双臂单测。
  - 批次 A 证据（commit 809fb81b）：真实 `npm pack --json --pack-destination <os tmp>`（tgz 落 OS 临时目录、stat 读压缩字节、不在工作树残留）；阈值冻结 ≥2MiB WARN（exit 0 + Owner 拆分决策记录提示语）/ >3MiB FAIL（exit 1 + 拆分 `@opentray/<name>-<os>-<arch>` 指令）；receipt 行含包名/tarball 路径/字节数/npm 版本/mode/platforms 文件清单；`--dry-run` 输出显式标注仅开发预警、不作发布证据、不给 packaging GO；默认发现规则=携带 `platforms/manifest.json` 的包（embedded staging 标记；ext-badge/ext-webview/ext-lynx 平台包形态不追溯）；package.json 挂 `check:pack-size`。bun fixture 四臂全绿：冻结边界分类（2MiB-1 ok/2MiB warn/3MiB warn/3MiB+1 fail）、真实 npm pack 小包 OK（actual tgz stat）、注入 pack+真实临时文件定长的 2.2MiB WARN 与 3.2MiB FAIL（exit 0/1）。目标 hash/解包 identity 检查随批次 D 5.1/5.2 release 预演收口。
  - 实现复核 r1 修复（2026-09-17，标准项）：两脚本按仓库 TypeScript/bun 规范移植——`scripts/check-pack-size.mjs` → `scripts/check-pack-size.ts`、`scripts/openspec/check-ext-dialog-sound-consistency.mjs` → `.ts`（导出/测试 import 同步迁移，`check:pack-size` 与 `verify:spec-consistency` 改 `bun run …ts`；.mjs 删除；verify-native-artifacts CI step 经 package.json script 不变）；bun 双套件 9/9 绿。

## 3. Implementation — 批次 B（crates/opentray-ext-dialog；批次 A 落地并过评审后开工）

- [ ] 3.1 macOS probe（**协议完成后前置**）：owner wake 饿死（Wait 无事件步进不停滞）/ exit race / step 与普通 menu frame 交错次序取证；未取得 probe 证据不得勾选 3.2。
- [ ] 3.2 macOS：show 登记 opaque operation + `beginModalSession` 立即 Accepted；`poll_owner` 驱动 `runModalSession` 步进状态机（一次性 completion CAS；teardown 先 CAS 再 endModalSession）；NSAlert（suppression/severity/escape→cancelId）/ NSOpenPanel / NSSavePanel；终帧 payload 恒带结果。
- [ ] 3.3 win32：per-owner 有界 STA worker（**数值冻结：cap 8 / 启动+进入超时 3s（**pre-Accept 失败 = 原 requestId 同步 typed error，不发 Accepted/终帧**）/ join 超时 2s（**worker 未退出不得 deinit/dlclose：保留引用至自然退出或放弃清理终止进程**）/ WM_APP close dispatcher**；**Accepted 诚实语义 = worker 已进入原生模态调用**，TaskDialog 以 TDN_CREATED 取证，IFileDialog 无先验呈现信号不谎称）；TaskDialogIndirect / IFileOpenDialog / IFileSaveDialog；cap-1/cap/cap+1 测试（满载 typed `dialog_worker_limit_reached` 前置拒绝，绝不静默排队）；线程亲和断言（command/deinit 恒 owner 线程，仅可拷贝数据与 port shim 跨线程；blanket Send 移除或证明永不移动）；deinit-after-join 断言。
- [ ] 3.3b comctl6 工程落点（R4 P1-3）：crates/opentray-bin Windows resource 文件（`.rc` 声明 common-controls v6 依赖）+ cargo build wiring（winres/embed-resource 选型按仓库现状）+ 启动探测日志/DTO fixture + packaged broker 真机证据。
- [ ] 3.4 busy 原子占用（同 scope 第二个 typed `dialog_session_busy`）；session close 先 CAS Revoked → 撤销 → cancel 分支 payload 终帧 → cleanup；四路 dismissal 一致映射。
- [ ] 3.5 BackendCapabilities DTO 嵌入上报 + 双 target CI + exhaustive fixture 比对。

## 4. Implementation — 批次 C（packages/ext-dialog facade）

- [ ] 4.1 `attachDialog(tray, options?)` → capability；`getBackend(): Promise<...>`（惰性 ensureLoaded 后请求 DTO，返回不可变快照；方法 dispatch 前 await 快照做能力前置）；`contract.json`；embedded 描述符。
- [ ] 4.2 facade preflight：Linux/命名空间 mismatch/未知字段/空 buttons/索引越界/路径类 typed 拒绝（状态变更前失败）；糖类型精确化（`Omit<...,"message"|"buttons"|"defaultId"|"cancelId">`；pickFile `multiple?: false` vs `multiple: true` overload，type-test 固定推断）。
- [ ] 4.3 vitest 确定性套件（Node+Bun）：糖语义、dismissal 映射、cancel=null、save 路径 canonicalize（存在 realpath / 不存在 leaf=parent canonicalize+lexical join）、命名空间矩阵、embedded 解析错误矩阵、typed 错误 wire round-trip + instanceof。

## 5. Packaging — 批次 D

- [ ] 5.1 native-build-graph 注册 `dialog` component + 收齐矩阵；**embedded staging manifest**（path/SHA-256/buildIdentity/version/fingerprint，root-contained）；release-plan/verify-native-plan/stage-release-artifacts/release.yml 同步；broker EXE RT_MANIFEST。
- [ ] 5.2 CI：check-pack-size 真实 pack 证据（tgz stat/版本/packlist/hash + 解包逐 target identity）写入 evidence artifact——无实测不给 packaging GO。

## 6. Verification

- [ ] 6.1 双平台真机验收：每方法冒烟；交错时间线取证（**同 session 多 tray/多 mount** + 跨 app 实例隔离；普通 set-menu/ext-command 交错完成）；四路 dismissal 一致性；断连/重复终帧/错 owner/旧 generation 竞态族；session close 撤销（payload=cancel 分支）；busy typed；suppression 回传；commandLink/expander 真机截证（win）。
- [ ] 6.2 全量门：workspace 测试 + typecheck + 双 target CI + vision validate + **check 绿（含 self-review 产物）** + **一致性门绿（已预接线：package.json `verify:spec-consistency` 挂入 `verify` 聚合器 + verify-native-artifacts CI step；语义冲突 fixture 测试为批次 A 任务 2.6b）**；clean checkout release 预演（真实 pack+解包）逐目标 identity check。
- [ ] 6.2b 一致性门 fixture 测试（**三臂已落地**：`scripts/openspec/check-ext-dialog-sound-consistency.test.ts` 4 用例——合法负面引用放行 / 伪装 marker 的语义冲突命中 / 每 ruleId 触发断言；随实现演进保持同步）。
- [ ] 6.3 self-review（md+html）+ check ok:true；Codex 复核轮（R3+）至 GO。

## 7. Release

- [ ] 7.1 AGENTS.md：Monorepo Law += 包体积门（已在工作树预落）；新章 Dialog Extension Law（design-reference §9 三条提炼，含 DeferredOperation 法则）。
- [ ] 7.1b 同步 `.agents/skills/develop-opentray-ext`：「one fat cross-platform native package」按体积门裁决修订。
- [ ] 7.2 skills/opentray 公共消费文档 + packages/ext-dialog/README。
- [ ] 7.3 changeset（minor）→ 合并 main → version → push → CI 全绿 → npm 上线（与 add-ext-sound 同波）。
