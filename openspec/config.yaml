schema: vision2

# Project context (optional)
# This is shown to AI when creating artifacts.
context: |
  Project: OpenTray.
  Product law: Tray-first Desktop Status Platform for Node/Deno/Bun CLI and AI-skill ecosystems.
  Architecture stance: caller-owned App identity, Tray status atoms, session-scoped isolation, and extension-driven native capabilities.
  Working style: use vision2 OpenSpec before implementation; keep interview_plan.md as the intent source of truth.

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
